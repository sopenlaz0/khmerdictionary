import SwiftUI

enum AppTheme {
    static let background = Color(red: 0.94, green: 0.96, blue: 0.99)
    static let cardBackground = Color.white.opacity(0.82)
    static let accent = Color(red: 0.04, green: 0.29, blue: 0.55)
    static let secondaryText = Color(red: 0.20, green: 0.31, blue: 0.45)
}
